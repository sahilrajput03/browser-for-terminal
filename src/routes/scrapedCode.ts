/**
    // Without using `sh` (shell) (limitation: we can only run one command at once only)
    // const [executable, ...options] = command.split(' ')
    // const { stderr, stdout, error } = spawnSync(executable, options);
*/